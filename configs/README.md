# Configuration provenance

`slowonly_r50_8xb32-u48-240e_k400-keypoint.py` is the recovered training configuration, preserved with its original hyperparameters and paths. It expects an MMAction2 checkout with the referenced base runtime and pose annotation files available locally.

The configuration does not include the pose-extraction pipeline, dataset preparation scripts, or annotation files. Do not treat its path strings as downloadable data locations.

